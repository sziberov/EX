<?
	// Outer: view_type_id, allow_advanced_control, id

	$allow_advanced_control ??= session_getSettings()['allow_advanced_control'] ?? false;

	if($object->type_id == 1) include 'actions_group';
	if($object->type_id == 2) include 'actions_user';
	if($object->type_id == 3) include 'actions_plain';
	if($object->type_id == 4) include 'actions_shared';
?>
<div>&nbsp;</div>