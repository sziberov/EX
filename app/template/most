<?
	$most = [
		D['title_most_popular'],
		D['title_most_visited'],
		D['title_most_commented'],
		D['title_most_recommended']
	];

	foreach($most as $most_id => $title) {
		$entities = entity_search('objects', 'Object_', null, object_getMostSearchCondition($most_id), 12)['entities'];

		include 'most.objects';
	}
?>