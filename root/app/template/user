<div attributes="_user={{if;[online]==true;online}} {{if;[mutual]==false;non-mutual-friend}}">
	<condition>
		<if>[avatar_url]!=</if>
		<then>
			<div _avatar="small" onclick="Hash.set('user/[user_login]')" title="[user_login]: [avatar_title]">
				<img src="[avatar_url]">
			</div>
		</then>
	</condition>
	<condition>
		<if>[user_login]!=</if>
		<then>
			<a href="#user/[user_login]">[user_login]</a>
			<condition>
				<if>[creation_time]!=</if>
				<then>
					<div>{formatTime &creation_time}</div>
					<condition>
						<if>[edit_time]!=</if>
						<then>
							<div>{formatTime &edit_time}</div>
						</then>
					</condition>
				</then>
			</condition>
		</then>
	</condition>
</div>